input int A[_];
int n = A.size;
int Y[n];

output int sum;

pardo(i : n) Y[i] = A[i];

for (int t = 0; 2 ^ t < n; t++) {
  @1;
  pardo(i : n / 2 ^ (t + 1))
    Y[i] = Y[2 * i] + Y[2 * i + 1];
}

sum = Y[0];

